package org.sgodden.echo.ext20.command;

import nextapp.echo.app.Command;

/**
 * A Web Application <code>Command</code> to force a Panel to perform a call to
 * doLayout
 */
public class DoPrintLayoutCommand implements Command {
}
